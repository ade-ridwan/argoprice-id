{
  "name": "agro-price",
  "workspaces": [
    "web"
  ],
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev -w web\" \"cd api && .venv/bin/python run.py\"",
    "install-all": "npm install && cd api && pip install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
